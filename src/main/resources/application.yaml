spring:
  application:
    name: NEPE

  datasource:
    url: jdbc:mariadb://localhost:3306/nepe_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: fede
    password: 3002FedericO2003
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none # Usiamo schema.sql per il controllo DDL esplicito
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
      data-locations: classpath:data.sql
